# 部署配置文件
# 请根据实际情况修改以下配置

# 服务器配置
SERVER_HOST="47.120.30.113"
SERVER_USER="root"  # SSH用户名
SERVER_DIR="/www/awesome-fund"  # 服务器上的部署目录
SSH_PORT="22"  # SSH端口

# 应用配置
APP_NAME="awesome-fund"
APP_PORT="3000"  # 应用运行端口

# PM2配置（如果使用PM2）
PM2_NAME="awesome-fund"
PM2_INSTANCES="max"  # 实例数量，max表示根据CPU核心数自动设置

# 日志配置
LOG_FILE="/var/log/awesome-fund.log"
ERROR_LOG_FILE="/var/log/awesome-fund-error.log"

# 环境变量（敏感信息，建议在服务器上单独配置）
# NODE_ENV="production"
